import { css } from '@emotion/core';

import { TASK_PAPER_BG } from '../../../../common/constants';

export const wrapperCSS = css`
  padding: 6px 8px 6px;
  margin-bottom: 8px;
  background-color: ${TASK_PAPER_BG};
  border-radius: 3px;
  box-shadow: 0 1px 0 rgba(9, 30, 66, 0.25);
`;
